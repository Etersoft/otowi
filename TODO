формировать config.h

собрать libs/wine libs/port
в переделанном виде оно в wineguts

Надо избавиться от ntdll

Вносить изменения в копию кода wine, чтобы иметь возможность там делать rebase

Часть кодировок уже была вынесена в natspec!
И детект локали тоже

Делать замену для принципиально не поддерживаемых функций, чтобы и не компилировалось?

Или stub с сообщением / выходом?

Перейти на динамическую библиотеку

WINEDEBUG=+all только так работает и только trace показывает

dlopen не работает с тем, что линковано ldconfig?
Wine/Windows смотрят на библиотеки, как на отдельные модули?
